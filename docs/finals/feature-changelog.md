# 본선 기능 변경 기록 (CUD Changelog)

> 본선(2026-07-04~05) 중 **새 사용자 기능·화면·엔드포인트·옵션**을 추가/변경/삭제하면 여기에 즉시 기록한다.
> 기능명세 제출 대상 판별의 단일 근거. **순수 최적화(품질·폴리시·프롬프트·렌더 개선)는 기록하지 않는다**(→ `optimization-report.md`).

| 일시 | 구분(C/U/D) | 무엇 | 왜 | 사용자 가시 변화 | 커밋 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-04 | D | 결제 표면 전면 폐기 — `/pricing` 페이지·랜딩 "결제" 메뉴·데모 결제 모달·`POST /deploy/demo-payment`·`_state.dev_pass` | 제품 목적(은행 내부 준법 마케팅 코파일럿)에 소비자 결제 서사 불일치 (사용자 지시) | 랜딩에서 결제 메뉴 사라짐, Deploy 발송 확정이 결제 모달 없이 바로 진행(권한 오류는 에러 텍스트). 엔타이틀먼트 토글(Setting)은 유지 | a53a788, 1a4e9bd |
| 2026-07-04 | C | Mock 자연 레이턴시 `DEMO_LATENCY_MS`(env, 기본 0=끔) | mock 즉답이 라이브 데모에서 인위적으로 보임 | 데모 서버에서 AI 답변·베이크가 자연스러운 대기 후 표시(텍스트 상한 1.2s / 이미지 상한 1.6s — 발표자 피드백 반영해 초기 2.5~3s에서 축소) | a53a788 |
| 2026-07-04 | U | 교정(revise) 재베이크를 image-edit(img2img)로 전환 + 비ko 언어 변형 fixture 3장 | 재생성마다 포스터가 통째로 바뀌어 "카피만 교정" 서사가 약함 · vi/en/zh가 PIL 폴백이라 ko와 품질 격차 | 검토 후 교정 시 기존 포스터 아트를 유지한 채 텍스트만 교체됨, 언어 전환 시 en/vi/zh도 ko와 동급 골드 캘리 포스터 | d69f899, (this) |
| 2026-07-04 | C | Review Studio에 독립 단계 RC(논란) 검토 노드 추가 — R0→R1→R2→**RC**→R3. 큐레이션 2티어 블랙리스트(`backend/app/references/controversy/blacklist.yaml`, 국립국어원 사전 존재 여부로 은어=critical/맥락어=warning 티어링, 중립 정체성어 제외, 숫자신호 경계매칭) 기반 결정론 안전망 + LLM 맥락 판정(Gemini) + 라이브 비전(OCR 역할)의 3경로 감지 | 배포 전 한국 사회 논란·평판 리스크를 정치적 판단 없이 중립적으로 사전 감지해 브랜드 리스크를 낮추기 위해 (사용자 지시) | 진행바에 "논란" 단계 노출 + 검토 화면 ControversyCard(치명/경고 배지·근거·선례 링크)로 findings 표시 + 배포전검토 화면 논란 건수 집계. 심각도 티어링으로 기존 게이트 합류(critical→BLOCKED, warning→WARN). mock 데모도 결정론 완주 | 0d1e90a, 8d3ebbe, e4fdc73, e3b485e, 6d3e2f0, f50bc4f, 03b9506, 33b107f, 66fa798 |
| 2026-07-04 | U | RC 논란 검토 라이브 비전 경로(경로3)에 시각 심볼·제스처 감지 추가 — 블랙리스트에 `visual_symbols`(욱일기 도안·전범기·나치 상징·집게손·일베 손가락) 신설, Gemini 비전 프롬프트가 텍스트/OCR로는 못 잡는 "그림·손동작"까지 판정하도록 지시 확장 | 욱일기는 문자열이 아니라 그림으로도 등장하고, 집게손 같은 논란 제스처는 애초에 텍스트가 아니라서 기존 OCR/블랙리스트 매칭으로는 원천적으로 탐지 불가 (사용자 지시) | 라이브 비전 판정 시 도안·제스처 findings가 기존 ControversyCard에 동일하게 표시(신규 UI 없음). 라이브 전용 best-effort — mock 데모 경로·기존 결정론 게이트는 변경 없음(집게손 등 제스처는 과잉 판정 방지 위해 warning으로 제한) | 57cbe4e |
| 2026-07-04 | C | JB 브랜드 지식 팩 신규 — `backend/app/references/brand/jb_group.yaml`(공식 CI 컨셉 PEOPLE·JB DIAMOND·SPACE·슬로건·실측 팔레트·로고 규정; 신뢰등급·출처 병기, as_of 스탬프. **products 섹션은 프롬프트 주입 금지**) + `backend/app/gateway/design/brand_context.py`(저변동 계층만 4~6줄 렌더, 부재 시 None 강하) | 생성 소재 기본 톤을 JB CI에 수렴시키되 시의성(상품·금리)은 팩에서 분리 — 시의성 SSOT는 런타임 | 없음(내부; 지시 없을 때만 생성 소재 기본 톤이 JB CI로 수렴) | 94f5863 |
| 2026-07-04 | U | Design S1 프롬프트 레퍼런스 말미에 브랜드 블록 append(사용자 지시·tokens 우선 명시) + History(RunList) 데모 표시 제목 3건을 현행 실상품으로(표시 전용) | 지시 없을 때만 JB CI 톤으로 수렴시키되 사용자 지시가 항상 우선 · 데모 표시 제목이 현행 실상품과 불일치 | 없음(지시 없을 때 기본 톤만 수렴) · History 목록의 데모 제목이 현행 실상품명으로 표시 | 92fc483, 122d37e |
| 2026-07-04 | C | "동작 원리" 아키텍처 소개 페이지 신설 — `/architecture`(시스템 흐름 8단계 자동 루프 다이어그램 · AI 에이전트 루프+4종 하네스 · 기술 스택 표) + 랜딩 네비 "동작 원리" 항목 | 심사 대응: 풀스택 구조·Agent 활용 방식을 비개발자도 이해하도록 제품 안에서 직접 설명 | 랜딩 상단 메뉴에 "동작 원리" 추가, 클릭 시 애니메이션 아키텍처 페이지(#flow·#agent·#stack) | (this) |
| 2026-07-04 | C | 스튜디오별 파일 업로드 소스화 — FileTree 업로드 버튼(Brain·Design·Review에서 노출) | Brain/Design/Review 각 스튜디오가 AI 소비 데이터(spec/plan·카피·심의 소재)를 자율 업로드 가능토록 제공 | 각 스튜디오의 FileTree에 업로드 버튼 추가, 업로드 파일은 `{studio}/uploads/`에 저장되어 AI 참조 · mock: 트리거 이미지 업로드 시 Review 결정론 적발(BLOCKED) 재현 가능 | (this) |
| 2026-07-04 | C | Review Studio 하이라이트 오버레이 — 신설 엔드포인트 `GET /runs/{id}/review-highlight`, verdict `location` 비파괴 확장(`bbox`/`image`), `HighlightFrame`(iframe srcDoc). Mock=정적 authored bbox(구절-tight)·Live=비전/OCR 소스 교체 구조 | 심의 결과가 텍스트 카드로만 설명돼 "이미지의 어디가 문제인지"를 시각적으로 못 짚음(시연 설득력·5.5 리스크 대응) | Review Studio 좌열 상단에 검토 포스터가 뜨고 위반 문구에 마커펜(치명=빨강/경고=노랑)+번호 핀이 그어져 근거 카드와 연결됨. 카드/시각 뷰 토글. bbox 없으면 기존 카드뷰로 축퇴 | (this) |
| 2026-07-04 | C | 업로드 이미지 하이라이트 — 외부 업로드 포스터 위반 지점 마커펜(Phase 1 오버레이 재사용, mock=지정 데모 authored bbox·live=비전 bbox). 백엔드 3점 변경: 호출4 seam(`_uploaded_location` finding bbox 보존)·mock 스텁 authored bbox(`UPLOAD_HIGHLIGHT_BBOX`)·비전 프롬프트 bbox 요청. 데모 자산 `external-deposit-promo.png`(위반 심음) 동봉 | 외부 소재 감사 시 위반 위치를 시각적으로 짚음(Brain/Design 없이 Review 단독 활용) | 업로드 이미지 리뷰 시 위반 문구에 마커펜+핀, 근거 카드 연결. 지정 데모 소재는 mock으로 하이라이트 재현. bbox 없으면 카드뷰로 축퇴 | (this) |
| 2026-07-04 | U | 업로드 소재 심의를 논란(RC) 카테고리로 라우팅 + 욱일기 데모 소재 신설 — 업로드 finding이 `category`(other_sensitive 등 블랙리스트 카테고리)를 달면 법률 도메인 화이트리스트 **비대상**으로 `controversy` 노드에 영속(→ ControversyCard). demo.py `_RISING_SUN_TRIGGERS`(해돋이·욱일·전범 등) 파일명 트리거 + 데모 포스터 `docs/finals/demo-assets/2026-신년-해돋이-적금.png`('일출/햇살'로 위장한 16선 욱일기, PIL 결정론 렌더) | 외부 포스터 업로드→검토 시연에서 "일출 디자인에 숨은 욱일기"를 RC 논란 카드로 적발하는 대표 사례 제공. 라이브 비전(파일명 무관 실도안 탐지) 경로의 mock 결정론 재현 | 해돋이 적금 포스터를 Review에 업로드→검토하면 ControversyCard에 "민감 상징·치명(욱일기 도안 근거)" 표시 + 게이트 BLOCKED + 리포트 RC 섹션에 근거. 신규 UI 없음(기존 논란 카드 재사용). 법률 §3 업로드 심의는 종전대로 유지 | (this) |
| 2026-07-04 | U | Review→Design 폐루프 완성 + 발송 내부통제 게이트 — R3 권장수정을 기계용 계약(revise/recommendations.json)으로 영속하고 Design 교정(remediate)이 이를 실소비(힌트 주입·고지 언어 지목·적용 내역 출처증빙), Review 게이트 버튼 원클릭화. deploy dispatch는 심의 PASS(또는 ack된 WARN) 없이는 409로 발송 거부 | 검증→개선이 하드코딩 데모가 아닌 실데이터 폐루프로 · 심의 게이팅을 UI가 아닌 백엔드가 강제(내부통제) | 교정 응답에 적용한 권장수정 목록 표시 · 심의 미통과 run은 발송 확정이 오류(409)로 차단 | (this) |
| 2026-07-05 | U | Mock 데모 GAP 일괄 폴리시 — ①챗 모델 기본값 Marker Pro(뷰 전환에도 유지) ②Setting 가격 표기 제거 ③시안 프리뷰: 티키타카 캘리그래피 헤드라인 즉시 표시(견본 프리스테이지)·브랜드 단계 후 실제 로고 인라인 ④언어 변형 포스터가 입력 이미지 패밀리로 위반/교정 단계 상속(교정본 오노출 제거) ⑤Review 배치 D: 시각근거 좌열 대형 + 카드 우열(겹침 제거)·헤드라인 bbox 재실측 ⑥하이라이트 대상을 합성 렌더(로고·고지 포함)로 승격 ⑦교정·재생성 후 판정 배지 "재검토 필요"로 전환(stale 게이트) ⑧변형 베이크 지연 생략·합성 업로드 다운스케일(레이턴시 단축) | 2026-07-05 새벽 발표자 실측 GAP 9건 + 지시 3건 반영 — 발표 리허설에서 프리뷰·심의 화면 불일치가 다수 발견됨 | 챗 기본 모델이 Marker Pro로 고정 표시 · Setting에 가격 없음 · 티키타카 직후 골드 캘리 헤드라인이 프리뷰에 보임 · 브랜드 후 프리뷰에 JB 로고 · 교정 전 en/vi/zh가 교정본으로 새지 않음 · Review 화면 2열(포스터 크게+카드) · 고지 경고가 고지가 실제 보이는 합성본 위에 표시 · 교정 후 BLOCKED 대신 "재검토 필요" | (this) |
| 2026-07-05 | U | Mock 포스터 fixture를 poc_E 실자산(교정 전/후 × 4언어 8장, 1084×1451~1792×2400 3:4)으로 전면 교체 + 시나리오 개정 — ①상품=JB 20대 청년 정기예금(최고 3.30% 세전=기본 2.80%+우대 0.50%p·6~36개월·100만원·만19~29세) ②위반 축 교체: 유일성·최상급("국내유일 최고"/Korea's No.1/tốt nhất/唯一最高)+절대보장("무조건 지급!"/No Conditions!) — 4개 언어 전부 스테이징 ③vi/zh 고지누락 아크 폐기(DISCLOSURE_DISPLAY vi/zh 표시문 추가) ④신규 RC 논란 아크: 교정 전 포스터의 **집게손 제스처**를 mock 비전([controversy-vision] 계약 마커+바이트 패밀리 판별)이 결정론 적발, 손 위치 bbox 하이라이트 ⑤로고 정책 `logo_policy: baked`(자산에 CI 정합 베이크 — S2c 오버레이·프리뷰 인라인 OFF, 이중 로고 방지) ⑥레이아웃 3:4(1080×1440)·티키타카=캘리 서브헤드+코발트(#1E63D0) 헤드라인 승격·하이라이트 bbox 언어별 실측 8종 | 기존 풀베이크 fixture 품질이 발표 수준 미달(사용자 자산 제작·교체 지시) — 실제 광고급 포스터로 mock 파이프라인 서사(모든 언어판 적발→원클릭 교정→ack→발송) 완성 | Design 산출 포스터가 실광고급 poc_E로 표시(4언어) · Review BLOCKED critical 8(언어당 2)+RC 집게손 경고(손 위치 마커) · 교정 후 클린 포스터(턱괴기 포즈)로 재베이크·논란 소멸 · WARN(고지비율) ack 후 발송 | (this) |
| 2026-07-05 | U | 교정(remediate) 후 자동 재검토 + 하이라이트 bbox 정밀화 | ①교정만 하고 판정이 "재검토 필요"(stale)로 멈춰 수동 재검토를 눌러야 하던 흐름이 어색(발표자 피드백: "돌아가서 프롬프트만 먹여도 통과되어야") ②en/vi/zh 위반 bbox가 근사치라 자기 포스터에서 어긋남 | 교정(원클릭 "리뷰 지적 반영해 재생성" 또는 Design 챗 "…교정해줘") 시 재베이크→씬 재조립→**재심의(R0→R3)까지 자동 완주**해 Review에 새 판정(PASS/WARN) 즉시 표시(수동 재검토 클릭 불필요). en/vi/zh 위반 헤드라인·금리카드 bbox를 각 언어 포스터 실측으로 정밀 보정 | (this) |
| 2026-07-05 | U | poc_E 경로 이중 오버레이 방지 — 고지·CTA도 로고와 동일하게 자산에 베이크 취급 | poc_E 포스터에 로고·고지·CTA가 모두 구워져 있는데(같은 맥락) 결정론 고지 오버레이를 또 얹어 이중 표기됨 | `logo_policy="baked"`인 spec에서 프론트 sceneAssembler·백엔드 layout_preview가 disclosure 오버레이(텍스트·스크림·시안 존)를 미방출. 레이아웃 spec의 disclosure 슬롯은 유지해 시각 적법성 룰(고지 존재·글자비율)은 그대로 통과. 배포 HF Space `DEMO_LATENCY_MS=0` 설정(mock 인위 지연 제거, set-env 워크플로) | (this) |
| 2026-07-05 | U | Mock 발송 마찰 제거 + Deploy Pro+ 게이트 폐기 | ①mock 시연에서 재검토가 vision_skipped(합성 렌더 부재) 등으로 WARN이 자주 떠 '경고 확인(ack)' 없이는 발송(dispatch)이 409로 막힘 ②Deploy가 Pro+ 엔타이틀먼트 잠금이라 데모 진입에 토글 필요 | dispatch 내부통제 게이트: **mock=true는 PASS/WARN 모두 ack 없이 발송 허용**(BLOCKED/미실행만 차단 — '위반 있으면 발송 못 함' 서사 유지), 실사용(mock=false)은 종전대로 PASS/ack된 WARN 필수. Deploy Pro+ 엔타이틀먼트 **완전 폐기** — Setting의 Deploy 토글 카드 삭제·DeployStudio 잠금 화면 제거·`entitlement.deploy` 항상 true. mock 데모는 BLOCKED→교정→재검토→**발송 확정 바로 성공** | (this) |
