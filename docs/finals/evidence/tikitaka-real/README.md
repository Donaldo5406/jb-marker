# 실 API 티키타카 + 디렉티드 풀베이크 라이브 검증 (2026-07-03, 충전 후)

로컬 8151(`DIRECTED_FULLBAKE=1`), provider=anthropic(텍스트)+google(이미지).
게이트 전부 ON으로 run a97e4965adc1 완주 + 교정 2건 반영 재베이크 2회.

## 티키타카 실증 (Mock으로 검증 불가했던 핵심)

- S1 게이트 프리뷰(`tk_real_s1.html`) 확인 후 챗 교정: "헤드라인을 '첫 목돈의 시작, JB와
  함께'로, 바디에 최고 연 3.30% 명시, CTA 우측 하단으로".
- 결과(`tk_real_s1_edit.html`): **3건 전부 정확 반영** — 헤드라인 문구 그대로, 바디에
  factsheet 수치(3.30%) 그라운딩 삽입, CTA bbox x 80→560. 이후 S2b가 교정 유지한 채
  전 수치(2.80%+0.50%p·6~36개월·100만원) 카피 확정. 프리뷰 즉시 재기록.

## 디렉티드 풀베이크 결함 발견 → 교정 → 재검증 (커밋 10ab27d)

| 산출물 | 상태 |
|---|---|
| `tk_real_s2a.png` (교정 전, 1K) | 카피·그라운딩·교정 반영 전부 정확. **결함 2건**: ①좌상단에 가짜 로고 베이크(safe zone 무시) ②최하단에 완전히 깨진 잔글씨 고지("사업에 베창의 잠은…") 베이크 — 절대규칙의 부정 지시만으로는 불충분 |
| `tk_fix_verify.png` (safe zone 격상 후, 1K) | 두 결함 해소(빈 로고 영역·무텍스트 하단). **신규 관찰**: 1K에서 혜택 칩 라벨 글리프 깨짐(100만원→"100만윈") — POC(2K)와의 해상도 차이가 원인 |
| `tk_final2k.png` (2K, run f53a3d68d588) | **양 교정 최종 검증**: safe zone 준수 + 칩 라벨 글리프 온전. 1856×2304 |

교정 내용: ① safe zone을 절대규칙(부정)에서 **레이아웃 항목(긍정: "빈 배경을 그려라"·
"글자 없는 단색 밴드를 그려라")으로 격상** — E 실측(밴드 지시는 깨끗이 따름) 근거.
② provider 계약에 `image_size` 옵션 스레딩(기본 None=현행 호출 동일), **directed만 2K**
— 계약 테스트 2건(off=None·on="2K")으로 잠금.

## 잔여 관찰 (정직)

- 금리 카드 라벨 중복이 실물로 확인됨: "최고금리 **최고 연** 3.30%"·"우대금리 **우대
  최대** 연 0.50%p" — T4 minor의 실물. facts 값이 한정어를 이미 포함할 때 라벨 생략
  로직(혜택 칩과 동일) 이식 가치 있음.
- 좌상단 예약 영역이 "빈 흰 박스"로 도드라짐 — 로고 오버레이가 얹히면 해소되나,
  베이크가 "박스" 대신 "밝은 배경 영역"으로 그리게 후속 다듬기 여지.
- 정보 밀도는 S1 spec이 만든 요소까지만(배지·캘리그래피 악센트·4칼럼 혜택 없음) —
  레퍼런스(image copy 6)급 밀도는 S1 spec 어휘 확장(v2)의 몫.
- 크레딧: Anthropic·Google 충전 확인(3키 프로브 OK). 이 검증 비용 ≈ 이미지 3장
  (1K×2+2K×1)+텍스트 턴들.
