"""POC: 전 요소 확정 → Gemini 이미지 모델에 풀 디렉팅 → 완성 포스터 원샷 베이크.
변형 A=원샷(로고만 참조), B=히어로 확정 후 참조. 사용법: python poc_fullbake.py A|B
"""
import pathlib
import re
import sys

from google import genai
from google.genai import types

SP = pathlib.Path(__file__).parent
ENV = pathlib.Path(r"C:\Users\jgqet\FinAI\jb-marker-personal\backend\.env")


def api_key() -> str:
    for line in ENV.read_text(encoding="utf-8").splitlines():
        m = re.match(r"\s*GOOGLE_API_KEY\s*=\s*(.+)\s*$", line)
        if m:
            return m.group(1).strip().strip('"').strip("'")
    raise SystemExit("GOOGLE_API_KEY not found in backend/.env")


DESIGN_SYSTEM_BLUE = """[디자인 시스템 — 전 요소가 이 시스템 하나로 통합될 것]
- 팔레트: 딥네이비 #0B2D6B, 브라이트블루 #1F6BFF, 배경은 밝은 하늘색에서 흰색으로 흐르는 부드러운 그라디언트, 카드는 순백. 모든 포인트 컬러는 블루 계열로 통일.
- 타이포그래피 3단 위계: ① 초대형 헤드라인 = 아주 굵은 모던 한글 산세리프, 브라이트블루 ② 캘리그래피 = 붓펜 느낌의 자연스러운 한글 손글씨(악센트 문구와 푸터 문구 전용, 작은 별·반짝임 장식 곁들임) ③ 본문 = 깔끔한 중간 굵기 산세리프, 네이비.
- 아이콘: 파스텔 블루+화이트 광택의 3D 렌더 아이콘 세트. 네 개 모두 동일한 시점·질감·라이팅으로 한 세트처럼 보일 것. 소프트 섀도.
- 사진 그레이드: 밝고 청량한 자연광, 프리미엄 은행 광고 화보 톤."""

LAYOUT_AND_COPY = """[레이아웃 — 위에서 아래 순서. 텍스트는 아래 지정된 문구를 글자 그대로]
1. 최상단 좌측: 첨부된 로고 이미지를 원본 그대로(다시 그리지 말 것) 작게 배치. 최상단 우측: 진한 파랑 원형 배지, 안에 흰 글씨 "20대라면 누구나!"
2. 그 아래 캘리그래피 손글씨 한 줄: "첫 목돈의 시작, 미래를 위한 가장 확실한 선택!"
3. 좌측 정렬 초대형 헤드라인 두 줄: 첫 줄 "JB 20대 청년" 둘째 줄 "정기예금"
4. 서브카피 두 줄: "청년의 꿈을 응원하는" / "JB금융그룹의 특별한 약속"
5. 좌측 중단: 흰색 라운드 금리 카드(부드러운 드롭섀도). 카드 내부 —
   - 최상단 작은 네이비 알약 배지: "JB 20대 청년 정기예금"
   - "최고 연" 라벨, 그 아래 카드에서 가장 큰 볼드 블루 숫자 "3.30%" 와 작은 "(세전)"
   - 얇은 회색 구분선
   - "기본금리 연 2.80% (세전)"
   - "+ 우대금리 최대 연 0.50%p (세전)"
6. 인물 히어로는 화면 우측 45%를 차지. 금리 카드·헤드라인과 겹치지 않게.
7. 하단 약 1/4 흰색 섹션: 중앙 타이틀 "JB 20대 청년 정기예금만의 특별한 혜택!" 아래 4칼럼(각 칼럼 = 3D 아이콘 + 볼드 타이틀 + 설명 한 줄, 칼럼 사이 얇은 세로 구분선):
   ① 과녁과 화살 아이콘 / "20대 우대금리" / "20대라면 누구나 우대금리 제공!"
   ② 체크 표시가 있는 달력 아이콘 / "기간 선택 자유" / "6개월~36개월 자유롭게 선택!"
   ③ 리본 달린 선물상자 아이콘 / "신규 고객 우대" / "첫 거래 고객 우대금리 혜택!"
   ④ 스마트폰 아이콘 / "비대면 가입 OK!" / "스마트뱅킹으로 간편하게 가입!"
8. 최하단 딥네이비 푸터 밴드: 좌측에 흰색 캘리그래피 손글씨 두 줄 "청년의 가능성을 키우는 금융," / "JB금융그룹이 함께합니다"
9. 푸터 밴드 안 맨 아래 아주 작은 연회색 고지 한 줄: "예금자보호법에 따라 5천만원까지 보호됩니다 · 기본금리 연 2.80% 세전, 12개월 기준"

[절대 규칙]
- 한글 텍스트는 단 한 글자도 틀리면 안 됩니다. 위에 지정한 문구만, 지정한 그대로 렌더하세요. 임의의 텍스트·숫자·외국어·워터마크·서명 추가 금지.
- 첨부된 로고는 그대로 합성하고 절대 다시 그리거나 변형하지 마세요.
- "사진 위에 패널을 얹은" 느낌 금지 — 처음부터 한 장으로 설계된 포스터처럼 모든 요소(사진의 색감, 아이콘 질감, 카드, 서체)가 서로 어울려야 합니다.
- 인쇄 검수를 통과한 최종 납품본 품질."""

PROMPT_A = f"""당신은 대한민국 1군 금융광고 스튜디오의 아트디렉터입니다. 첨부된 로고를 사용해, 아래 명세를 그대로 따른 프리미엄 은행 포스터 한 장을 완성하세요.

[포맷] 세로 3:4 포스터, 고해상도 인쇄용.

{DESIGN_SYSTEM_BLUE}

[인물 히어로 — 직접 촬영 디렉팅]
밝게 미소 짓는 20대 한국인 남성, 연한 블루 니트, 책상에 한 팔을 괴고 카메라를 바라봄. 곁에 파란 돼지저금통 소품. 배경은 크게 블러 처리된 밝은 도시 스카이라인. 인물 피부톤 자연스럽게, 손가락 왜곡 금지.

{LAYOUT_AND_COPY}"""

PROMPT_B = f"""당신은 대한민국 1군 금융광고 스튜디오의 아트디렉터입니다. 첨부된 1번 사진은 확정된 히어로 컷이고, 2번 이미지는 브랜드 로고입니다. 이 히어로 컷을 중심에 두고 아래 명세대로 프리미엄 은행 포스터 한 장을 완성하세요.

[포맷] 세로 3:4 포스터, 고해상도 인쇄용.

[히어로 사용 규칙]
- 1번 사진의 인물·표정·구도·골든아워 무드를 최대한 보존하며 포스터의 중심 비주얼로 통합하세요(재촬영하듯 완전히 다시 그리지 말 것).
- 디자인 시스템은 이 사진에서 파생: 골든아워의 따뜻한 앰버 + 신뢰의 딥네이비 #0B2D6B 투톤. 카드·하단 섹션은 순백으로 대비. 포인트 블루 #1F6BFF는 배지·숫자에만.
- 타이포·아이콘·카드 규칙은 아래와 동일하되, 전체 색 온도가 사진과 조화되도록 그레이딩.

- 타이포그래피 3단 위계: ① 초대형 헤드라인 = 아주 굵은 모던 한글 산세리프 ② 캘리그래피 = 붓펜 느낌 한글 손글씨(악센트·푸터 전용) ③ 본문 = 중간 굵기 산세리프.
- 아이콘: 화이트+앰버·네이비 포인트의 광택 3D 렌더 아이콘 세트, 동일 시점·질감.

{LAYOUT_AND_COPY}"""


# 일반화 검증 — 전혀 다른 명세(달러예금·프리미엄·세리프·골드 라인 아이콘·3칼럼).
# factsheet(가정): 최고 연 4.10%(USD 세전)=기본 연 3.80%+우대 0.30%p, 기간 1~12개월,
# 최소 USD 1,000, 환율 우대 90%.
PROMPT_C = """당신은 대한민국 1군 금융광고 스튜디오의 아트디렉터입니다. 첨부된 로고를 사용해, 아래 명세를 그대로 따른 프라이빗뱅킹급 프리미엄 외화예금 포스터 한 장을 완성하세요.

[포맷] 세로 3:4 포스터, 고해상도 인쇄용.

[디자인 시스템 — 앞의 청년 상품과 완전히 다른, 절제된 프리미엄 무드]
- 팔레트: 딥네이비 #0A1F3C 바탕, 샴페인 골드 #C9A24B 포인트, 아이보리 텍스트. 화려한 원색 금지.
- 타이포그래피: ① 헤드라인 = 우아한 한글 세리프(명조 계열), 아이보리 ② 악센트 = 가는 세리프 이탤릭 느낌 한 줄 ③ 본문 = 가는 산세리프. 자간 여유, 미니멀.
- 장식: 얇은 골드 라인·모서리 프레임만. 3D 아이콘 금지 — 아이콘은 1.5px 두께의 골드 라인 아이콘으로 통일.
- 사진: 야경의 글로벌 시티 스카이라인(맨해튼 느낌)을 배경으로, 창가에 선 우아한 40대 한국인 여성의 옆모습 실루엣(다크 정장), 손에 커피잔. 시네마틱 로우키 조명.

[레이아웃 — 위에서 아래 순서. 텍스트는 지정 문구 그대로]
1. 최상단 중앙: 첨부된 로고를 원본 그대로 작게(밝은 배경 알약 안에 배치해 가독 확보). 그 아래 작은 골드 태그 "USD 외화예금"
2. 가는 세리프 악센트 한 줄: "달러 강세 시대, 자산의 품격을 더하다"
3. 중앙 정렬 초대형 세리프 헤드라인: "JB 달러 정기예금"
4. 서브카피 한 줄: "글로벌 자산 포트폴리오의 시작"
5. 중단: 인물·야경이 넓게 보이는 영역(텍스트 없음).
6. 하단 1/3: 얇은 골드 라인 테두리의 반투명 다크 카드 —
   - "최고 연" 라벨과 초대형 골드 숫자 "4.10%" 와 작은 "(USD, 세전)"
   - 아래 한 줄: "기본금리 연 3.80% + 우대금리 최대 연 0.30%p"
7. 그 아래 3칼럼(골드 라인 아이콘 + 타이틀 + 설명 한 줄, 칼럼 사이 얇은 골드 세로선):
   ① 환전 화살표 라인 아이콘 / "환율 우대 90%" / "환전 수수료 부담을 낮췄습니다"
   ② 모래시계 라인 아이콘 / "기간 선택 자유" / "1개월부터 12개월까지"
   ③ 스마트폰 라인 아이콘 / "비대면 가입" / "모바일로 간편하게 가입"
8. 최하단 아주 작은 회색 고지 한 줄: "예금자보호법에 따라 5천만원까지 보호됩니다(원화 환산 기준) · 환율 변동에 따라 원화 환산 수익이 달라질 수 있습니다"

[절대 규칙]
- 한글 텍스트는 단 한 글자도 틀리면 안 됩니다. 지정 문구만, 지정한 그대로. 임의 텍스트·외국어·워터마크 금지.
- 첨부 로고는 그대로 합성, 재그리기 금지.
- 전 요소가 하나의 프리미엄 디자인 시스템으로 통합될 것. 인쇄 검수 통과 품질."""


# D = 목표 플로우 검증: 디자인 시스템(아이콘 시트) 먼저 확정 → 확정 시트를 참조로 최종 베이크.
PROMPT_D1 = """금융 광고용 아이콘 시트 한 장을 만드세요. 검수용 스타일 가이드 시트입니다.

[포맷] 딥네이비 #0A1F3C 단색 배경, 2x2 그리드로 아이콘 4개, 각 아이콘 아래 작은 아이보리 라벨.
[아이콘 스타일] 샴페인 골드 #C9A24B 단색, 일정한 1.5px 두께 라인 아이콘, 장식 최소, 기하학적으로 완벽하게.
[아이콘 4종]
1. 원형 순환 화살표 안에 정확한 달러 기호 "$" (라벨: "환율 우대") — $는 세로획 하나에 S가 또렷해야 함
2. 모래시계 (라벨: "기간 선택")
3. 체크 표시가 화면에 있는 스마트폰 (라벨: "비대면 가입")
4. 방패 안에 체크 표시 (라벨: "예금자 보호")
[규칙] 지정한 라벨 외 텍스트 금지. 4개 모두 동일 두께·동일 시점으로 완벽한 세트."""

PROMPT_D2 = PROMPT_C.replace(
    """   ① 환전 화살표 라인 아이콘 / "환율 우대 90%" / "환전 수수료 부담을 낮췄습니다"
   ② 모래시계 라인 아이콘 / "기간 선택 자유" / "1개월부터 12개월까지"
   ③ 스마트폰 라인 아이콘 / "비대면 가입" / "모바일로 간편하게 가입\"""",
    """   ① 첨부된 아이콘 시트의 '환율 우대' 아이콘을 그대로 사용 / "환율 우대 90%" / "환전 수수료 부담을 낮췄습니다"
   ② 첨부된 아이콘 시트의 '기간 선택' 아이콘을 그대로 사용 / "기간 선택 자유" / "1개월부터 12개월까지"
   ③ 첨부된 아이콘 시트의 '비대면 가입' 아이콘을 그대로 사용 / "비대면 가입" / "모바일로 간편하게 가입\"""",
) + "\n- 두 번째 첨부 이미지는 확정된 아이콘 시트입니다. 아이콘은 시트의 형태를 그대로 따라 그리세요(재해석 금지). 시트 자체를 포스터에 넣지는 마세요."


def main() -> None:
    variant = (sys.argv[1] if len(sys.argv) > 1 else "A").upper()
    client = genai.Client(api_key=api_key())
    logo = (SP / "vc_logo.png").read_bytes()
    if variant == "A":
        contents = [types.Part.from_bytes(data=logo, mime_type="image/png"), PROMPT_A]
    elif variant == "C":
        contents = [types.Part.from_bytes(data=logo, mime_type="image/png"), PROMPT_C]
    elif variant == "D1":
        contents = [PROMPT_D1]
    elif variant == "D2":
        sheet = (SP / "poc_D1.png").read_bytes()
        contents = [types.Part.from_bytes(data=logo, mime_type="image/png"),
                    types.Part.from_bytes(data=sheet, mime_type="image/png"), PROMPT_D2]
    else:
        hero = (SP / "vc_hero.png").read_bytes()
        contents = [types.Part.from_bytes(data=hero, mime_type="image/png"),
                    types.Part.from_bytes(data=logo, mime_type="image/png"), PROMPT_B]
    # 2K 우선(잔글씨 가독), 미지원 SDK/모델이면 aspect만으로 폴백
    try:
        cfg = types.GenerateContentConfig(
            image_config=types.ImageConfig(aspect_ratio="3:4", image_size="2K"))
        resp = client.models.generate_content(model="gemini-3-pro-image", contents=contents, config=cfg)
    except Exception as e:
        print("2K config 실패, aspect만으로 재시도:", type(e).__name__)
        cfg = types.GenerateContentConfig(image_config=types.ImageConfig(aspect_ratio="3:4"))
        resp = client.models.generate_content(model="gemini-3-pro-image", contents=contents, config=cfg)
    for part in resp.candidates[0].content.parts:
        inline = getattr(part, "inline_data", None)
        if inline and getattr(inline, "data", None):
            out = SP / f"poc_{variant}.png"
            out.write_bytes(inline.data)
            print("saved", out, len(inline.data), "bytes")
            return
    raise SystemExit("응답에 이미지 파트 없음: " + str(getattr(resp, "text", ""))[:300])


if __name__ == "__main__":
    main()
