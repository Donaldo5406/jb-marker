# Mock 파이프라인 개편 — 라이브 리허설 증빙 (2026-07-03)

로컬 dev(프론트 3100 · 백엔드 8151, `DIRECTED_FULLBAKE=1`, **Mock ON — 키·과금 0**)에서
개편된 mock 데모 대본을 브레인스토밍→발송 확정까지 완주한 기록. run `ceaa95aeb665`
(01~04는 동일 동작의 선행 run `84c9ea1c59dd` 캡처).

## 데모 대본 (본선용 — 순서대로)

⚠️ **함정 2개**: ① 챗 모델 셀렉터는 뷰 전환 때마다 "Claude"로 리셋됨 — **매번 "Marker Pro"
재선택 필수**(안 하면 일반 어시스턴트로 빠짐) ② Setting에서 **Mock ON + Deploy 엔타이틀먼트
ON** 선행.

| # | 조작 | 화면 확인 | 증빙 |
|---|---|---|---|
| 1 | "정기예금 캠페인 기획하자" | 웹 리서치 3건이 트리에 쌓이고 타겟 질문 | 01 |
| 2 | "2030 사회초년생" → "영어+베트남어+중국어" | **AI 디자인 디렉션 제안 턴**: 리서치 근거로 A안(그린+골드·타이포 위계·라인 아이콘) 권장 + 옵션 3 | 02 |
| 3 | "A안 (권장)" → "예, plan으로" → "보충하기" → "예, 확정" | spec 확정(디렉션 반영 ack) → plan 1차 누락→보충 완성 | — |
| 4 | Design 이동 → "plan 읽기 시작" | S1 게이트: **시안 프리뷰**(팔레트·금리카드·레이아웃 존) 중앙 렌더 | 03 |
| 5 | 챗: "헤드라인을 붓펜 캘리그래피 느낌의 골드로 키워줘" | **티키타카**: 프리뷰의 비주얼 콘셉트·헤드라인 존이 골드(#FFD166)·88px로 즉시 변화 | 04 |
| 6 | 확정&다음 ×2 (카피→비주얼) | 위반 카피 스테이징("업계 최고"·4.0%) → **poster_violating_gold**(붓펜 캘리 골드, 실 Gemini 2K 사전 베이크) 등장. 베이크 후 프리뷰는 오버레이 슬롯(로고·고지)만 표시 | 05(HTML)·06 |
| 7 | 확정&다음 ×2 (브랜드→Final) | 로고·고지 오버레이가 얹힌 4언어 씬 — 좌상단 JB 로고가 백판 없이 자연스럽게 안착 | 07 |
| 8 | Review 이동 → 검토 시작 | **BLOCKED — critical 4/warning 2**: 과장광고(표시광고법§3)·금리불일치·vi/zh 예금자보호 고지 누락 + **티키타카 2차효과**(고지 26px < 88px의 30%, 금투협§5④) — 전부 law.go.kr/kofia 실 딥링크 | 08 |
| 9 | **"리뷰 지적 반영해 재생성" 클릭 — 원클릭**(2026-07-05 폐루프: Design 자동 전환+교정, 챗 타이핑 불필요. 보조 경로로 챗 "교정해줘"도 유지 — "수정"은 시그널 미발동 주의) | 챗에 **출처 증빙** "리뷰 권장수정 N건을 반영해 카피·고지를 교정했습니다: rec_xxxx(vi)…" + 카피 교정 + 고지 보강(리뷰 지목 언어 vi/zh) + **재베이크 → poster_v2**(fixture 바이트 일치) | 09* |
| 10 | Review → 재검토 → **"경고 확인 후 진행"(ack) 필수 클릭** | **critical 0** (경고 1 잔존 = 고지 비율 — 티키타카 2차 효과 서사). ⚠️ 2026-07-05부터 **ack은 발송의 백엔드 필수조건**(미클릭 시 12번에서 409) | 10 |
| 11 | Deploy: 이메일 채널 → 적법성 검사 | §50(수신거부 23·무동의 20)·§15/16(목적외 26·기간초과 18) → 512명 중 425 발송대상, 야간차단 시간대 시각화 | 11·12 |
| 12 | 전체 패키징(4언어 ok) → 발송 확정(시뮬) *(결제 단계는 2026-07-04 폐기)* | ko 144·vi 141·zh 140·en 0 = 425명 [STUB] 발송 기록 + Deploy Report | 13 |

## 2026-07-05 갱신 — 폐루프·발송 게이트 반영 (PR #13, main=3634941)

- **9번 아크 변경**: BLOCKED/WARN 버튼이 "리뷰 지적 반영해 재생성"으로 — 클릭 한 번에
  Design 전환→R3 권장수정(revise/recommendations.json) 실소비 교정→증빙 챗 노출.
  증빙 09* 캡처는 구 아크(버튼+챗 타이핑) 기록 — 산출물(poster_v2·고지 보강)은 동일.
- **발송 백엔드 게이트**: dispatch가 심의 PASS/ack된 WARN 외 **409** 거부(사유 화면 표시).
  10번 ack 생략 시 12번이 막힌다 — 대본 순서 준수.
- **내부통제 시연 옵션(+10초)**: 심의를 건너뛴 별도 run에서 발송 확정 시도 →
  "review gate: not_run — 심의(PASS)를 통과해야 발송할 수 있습니다" 표시.
  "게이트를 UI가 아니라 서버가 강제한다"는 킬러 포인트로 활용 가능.

## 검증된 개편 항목 (라이브)

- 2×2 fixture 매칭: 1차 베이크 v1.png == `poster_violating_gold.png` **바이트 일치**,
  교정 재베이크 == `poster_v2.png` **바이트 일치**.
- 프리뷰 이중 텍스트 수정(356fcff): 베이크 후 레이아웃 섹션이 로고·고지만 방출 —
  베이크된 headline/body/cta 박스가 포스터 위에 겹치지 않음.
- Stage A 4턴(제안 턴) · S1 티키타카 → 프리뷰 즉시 갱신 · 위반→교정→재검토 아크 ·
  배포 전 구간이 전부 결정론으로 재현됨.

## 알려진 한계 (정직)

- 티키타카 시그널은 대본 키워드("캘리"/"골드")에만 반응 — 자유 지시는 실 API 모드의 몫.
- 프리뷰는 결정론 스키매틱: 색·크기·배치만 반영, 붓펜 질감 등 서체 렌더는 불가
  (self-contained 제약 — 질감은 베이크 산출물이 보여줌).
- 티키타카(88px) 수행 시 재검토가 PASS가 아닌 **WARN(critical 0)** — 고지 26px가 30%
  기준에 0.5%p 미달 + 합성 렌더 미시드(vision_skipped). 데모에선 "타이포 변경의 2차
  효과까지 잡는다"로 서술하고 "경고 확인 후 진행". 티키타카 생략 시(72px)에는 고지
  비율 경고가 없음.
- ~~비ko 언어 변형 포스터(v1.en 등)는 PIL 합성 폴백~~ → **해소(2026-07-04)**: v2 베이스에서
  텍스트만 언어 교체한 실생성 fixture 3장(`poster_v2_{en,vi,zh}.png`, 사용자 제작) — 골드
  경로의 비ko 변형이 ko와 동급 품질. 비골드 경로(티키타카 생략)의 비ko는 여전히 PIL 폴백.
- 발송·결제는 명시적 STUB/시뮬 라벨.
- 백엔드 재기동 시 로컬 store run 소멸 — 본선 당일 서버를 먼저 띄우고 run을 만들 것.
