# 프로덕션 빌드 전용 공개 설정(버전관리). 로컬 dev는 .env.local(localhost) 사용.
# 백엔드(HF Space) 주소를 여기에 고정해, Vercel 대시보드 env가 비어 빌드가
# localhost 폴백으로 회귀하던 문제를 방지. NEXT_PUBLIC_* 는 빌드타임 인라인이라
# 값 변경 시 재배포 필요. (Supabase 공개키는 Vercel 대시보드 env로 주입)
NEXT_PUBLIC_API_BASE=https://doss-8b-instruct-jb-marker.hf.space
